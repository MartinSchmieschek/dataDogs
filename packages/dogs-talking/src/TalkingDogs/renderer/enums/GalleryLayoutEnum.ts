/**
 * GalleryLayoutEnum.ts — The Catalogue of Forbidden Visions
 *
 * Arr, the gallery be a collection of images plundered from realms
 * beyond mortal ken. In luminous space blackened stars, they gaze,
 * accuse, deny — and each thumbnail be a window into the abyss, matey.
 */

/** Arr, the gallery's cursed slots — title, thumbnails, and the button that opens the void's collection. */
export enum GalleryLayoutEnum {
    Title = "Title",
    Thumbnails = "Thumbnails",
    OpenGallery = "OpenGallery",
  }
