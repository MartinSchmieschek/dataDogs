---
name: slopdogs-mcp-gateway
description: Points to mcp/skill.md, the instructions the SlopDogs MCP server injects at initialize (Spuren-Pflicht, tone, first-interaction protocol, MCP tools, ACL, waves, compositor pattern). Use when working through the MCP gateway, SlopDogs MCP tools, or matching server-delivered agent behavior; attach when API-only workflow is not enough.
disable-model-invocation: true
---

# SlopDogs — MCP Skill (Verweis)

Die einzige Quelle ist [`mcp/skill.md`](../../../mcp/skill.md) — derselbe Text, den der MCP-Server als Resource `slopdogs://skill` ausliefert (Kurzfassung bei `initialize`: `mcp/spuren-brief.ts`).
Lies dort; diese Datei spiegelt nichts mehr, damit es keine zweite Wahrheit gibt.
